# Golden Corpus: Hardcoded Secrets (CWE-798) — Python
# MUST NOT trigger the rule

import os

_api_key = os.environ.get("API_KEY")
_stripe_secret = os.getenv("STRIPE_SECRET")
_db_url = os.environ["DATABASE_URL"]

# Configuration flags, not secrets
DEBUG_MODE = True
MAX_RETRIES = 3
TIMEOUT = 30
DEFAULT_PAGE_SIZE = 50


# Not a secret — just a config flag
DEBUG_MODE = True
MAX_RETRIES = 3
